## What this changes and why

## Checklist
- [ ] `python3 -m unittest discover -s tests -v` passes
- [ ] English is used throughout (code, comments, docs); if a changed
      root `.md` file has a Spanish counterpart under `docs/es/`, it's
      updated too (or noted here if not)
- [ ] If this adds a new MIDI controller: the change is contained to
      `src/adapter/` (see `CONTRIBUTING.md`)
